name: gymsas_auth
description: Módulo reutilizable de autenticación para aplicaciones GymSAS.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.4
  gymsas_api_client:
    path: ../gymsas_api_client

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: false
